Mattermost bulk-apply examples

These examples intentionally exclude `.env` sync.

Included:
- `mattermost-core-config`
- `mattermost-sso-config`
- `mattermost-systemd-override`
- `mattermost-apply` workflow

Files are split into:
- `*.json` for strict metadata
- `*.body` for rendered target content
